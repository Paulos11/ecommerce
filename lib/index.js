const modelConfig = {
  timestamps: true, //to add created time and updated time
  autoIndex: true,
  autoCreate: true, //without any data collection will be created
};

module.exports = { modelConfig };
